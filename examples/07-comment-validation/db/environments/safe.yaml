name: safe
database_url: "postgresql://localhost/example_comment_safe"
include_dirs:
  - db/schema

build:
  validate_comments:
    enabled: true  # Validation enabled
    fail_on_unclosed_blocks: true
    fail_on_spillover: true
  separators:
    style: block_comment  # Safer style
