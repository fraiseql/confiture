[project]
name = "fraiseql-confiture"
version = "0.54.0"
description = "PostgreSQL schema evolution with built-in multi-agent coordination 🍓"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "evoludigit", email = "lionel.hamayon@evolution-digitale.fr"}
]
keywords = [
    "postgresql",
    "migration",
    "database",
    "schema",
    "ddl",
    "coordination",
    "multi-agent",
    "collaboration",
    "conflict-detection",
    "ai-agents",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Database",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Version Control",
]

dependencies = [
    "typer>=0.12.0",
    "rich>=13.7.0",
    "pydantic>=2.5.0",
    "pyyaml>=6.0.1",
    "psycopg[binary,pool]>=3.1.0",
    "sqlglot>=28.0",
    # PostgreSQL's own parser (libpg_query) — the one parser since 0.50.0 (D13).
    # Uncapped since 0.39.0 (#192): enum members are resolved by name through
    # `core/_pglast_enums.py`, `tests/unit/test_pglast_enum_binding.py` guards the
    # binding, and CI runs the AST suites on both ends of the range.
    "pglast>=6.0",
]

[project.optional-dependencies]
testing = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
    "pytest-json-report>=1.5.0",
    "pytest-xdist>=3.0.0",
]

dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
    "pytest-xdist>=3.0.0",  # -n / --dist=loadfile: addopts assumes it is installed
    "pytest-watch>=4.2.0",
    "pytest-json-report>=1.5.0",
    "ruff>=0.6.0",
    "ty>=0.0.7",  # Astral's ultra-fast type checker (replaces mypy)
    "maturin>=1.7.0",
    "fastapi>=0.111",
    "httpx>=0.27",
    "jsonschema>=4",  # Validate JSON output schemas in docs/reference/json-schemas/
]

fraiseql = [
    # FraiseQL integration (requires Python 3.13+)
]

# fraiseql-uuid is the CANONICAL home for the structured pattern-UUID convention
# (the {table}{type}-{func}-4{scen}-8{...}-{inst} v4-compliant encoding). Confiture's
# seed-validation only does generic RFC-4122 *format* checks and deliberately keeps
# NO parallel copy of that convention — the old uuid_patterns/uuid_validator island
# was deleted (ECO-rec2). This OPT-IN extra pulls in the canonical definition for
# projects that want structured-pattern validation, and lets the convention-seam
# guard test (tests/unit/test_uuid_convention_seam.py) assert alignment when present.
# Core install never requires it. It IS present in the type-check leg, which syncs
# --all-extras; the test legs sync only dev/notifications/ast, so the seam test's
# alignment half stays skipped there.
seed-uuid = [
    "fraiseql-uuid>=0.1.3",
]

# Empty alias since 0.50.0: pglast is a dependency. `fraiseql-confiture[ast]` still
# resolves for one release; the extra is removed in 1.0.0.
ast = []

mcp-http = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
]

docs = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.26",
    "pymdown-extensions>=10.0",
]
notifications = [
    "jinja2>=3.1",
]

[project.urls]
Homepage = "https://github.com/fraiseql/confiture"
Documentation = "https://github.com/fraiseql/confiture"
Repository = "https://github.com/fraiseql/confiture"
Issues = "https://github.com/fraiseql/confiture/issues"
FraiseQL = "https://github.com/fraiseql/fraiseql"

[project.scripts]
confiture = "confiture.cli.main:app"

[project.entry-points."pytest11"]
confiture = "confiture.testing.pytest_plugin"

[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[tool.maturin]
python-source = "python"
module-name = "confiture._core"
features = ["extension-module"]
include = [
    { path = "python/confiture/schemas/*.json", format = "wheel" },
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
pythonpath = ["python"]
addopts = [
    "--strict-markers",
    "--tb=short",
    # Under -n N, keep every test of a module on one worker: modules that create
    # server-global objects (roles) reuse fixed names, and per-worker databases
    # only isolate what lives inside a database.
    "--dist=loadfile",
    # Wall-clock assertions pass or fail with the load on the machine; they run
    # only when asked for (`-m benchmark`, the performance workflow).
    "-m",
    "not benchmark",
]
markers = [
    # Layer markers — assigned from the directory by tests/conftest.py.
    "unit: Unit tests (no database required)",
    "integration: Integration tests (require database)",
    "e2e: End-to-end tests (full workflows)",
    "performance: Performance suites (tests/performance)",
    "contract: Adapter contract tests (tests/contract)",
    # Orthogonal markers.
    "slow: Slow tests (skip in fast CI)",
    "benchmark: Asserts an upper bound on a measured duration; excluded by default",
]

[tool.coverage.run]
source = ["python/confiture"]
omit = [
    "*/tests/*",
    "*/__pycache__/*",
    "*/site-packages/*",
    "*/testing/fixtures/*",  # Test infrastructure, not application code
]

[tool.coverage.report]
precision = 2
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["python"]
# One extractor-coverage fixture is deliberately unparsable (it pins the
# `syntax_error` warning); ruff cannot lint what it cannot parse.
extend-exclude = ["tests/fixtures/idempotency_shapes/db/migrations/20260101000030_syntax_error.py"]

[tool.ruff.lint]
# Deliberate, focused ruleset (see ARCH-M3). Confiture runs a curated core
# rather than the full prescribed superset: the families below catch real bugs
# and style drift with near-zero false positives. The heavier / more opinionated
# families (PL, PERF, FURB, ERA, PTH, TCH, RUF) are intentionally NOT enabled —
# enabling the full set surfaces ~900+ findings (much of it churn) without
# commensurate value today. Adopt them incrementally, family-by-family, if a
# future need earns it. This config — not the global methodology — is the
# source of truth for Confiture's lint scope.
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "ARG", # flake8-unused-arguments
    "SIM", # flake8-simplify
    "RUF",  # ruff-specific rules (Phase 11, D6); RUF100 alone since Phase 08
    "ERA",  # commented-out code (Phase 11, D6): prose comments that parse as code are reworded
    "PTH",  # pathlib over os.path (Phase 11, D6)
    "PERF",  # perflint (Phase 11, D6)
    "PL",  # pylint (Phase 11, D6); design metrics baselined in tests/budgets.json
    "C901",  # McCabe complexity 15; existing offenders baselined in tests/budgets.json
]
ignore = [
    "E501",  # line too long (handled by formatter)
    "B008",  # do not perform function calls in argument defaults
    "UP042",  # StrEnum migration changes str() semantics; code/tests rely on the
    # (str, Enum) form (e.g. str(LintSeverity.ERROR) == "LintSeverity.ERROR").
    # Typographic characters (ℹ → — …) in user-facing strings, docstrings and comments
    # are deliberate; the confusable-character rules would flag every one of them.
    "RUF001",
    "RUF002",
    "RUF003",
    # Function-level imports are a per-file budget that only shrinks
    # (tests/unit/test_lazy_imports_justified.py), with a `# Reason:` on every kept one.
    "PLC0415",
]

[tool.ruff.lint.mccabe]
max-complexity = 15

[tool.ruff.lint.pylint]
max-args = 8
max-branches = 15
max-statements = 60
max-returns = 8

[tool.ruff.lint.per-file-ignores]
# Allow assert statements and nested with statements in tests
# SIM117: Nested with statements are often clearer in tests with many patches
# SIM105: try-except-pass is acceptable for "expected to fail" test scenarios
# Test bodies are linear scripts: magic values and their length are not design smells.
"tests/*" = ["ARG", "S101", "SIM117", "SIM105", "PLR2004", "PLR0911", "PLR0912", "PLR0913", "PLR0915", "C901"]
# Migration subclasses set hook/precondition lists as plain class attributes — that is
# the Migration API as documented; the examples show it as users write it.
# Examples and one-off scripts are linear demonstrations: literal values and length are
# the point, not a design smell.
"examples/*" = ["RUF012", "PLR2004", "PLR0915"]
"scripts/*" = ["PLR2004", "PLR0915"]
# Extractor-coverage fixtures pin argument *shapes*, some of them deliberately
# wrong: an unbound name (F821) and %-formatting (UP031) are the shapes.
"tests/fixtures/idempotency_shapes/*" = ["F821", "UP031", "PLW0603", "PLC0206"]
# BEGIN GENERATED: budget-ignores (scripts/budgets.py --update; do not edit)
"python/confiture/cli/commands/admin.py" = ["PLR0913"]
"python/confiture/cli/commands/debug.py" = ["PLR0912"]
"python/confiture/cli/commands/drift.py" = ["C901", "PLR0912", "PLR0913"]
"python/confiture/cli/commands/migrate/baseline.py" = ["PLR0912", "PLR2004"]
"python/confiture/cli/commands/migrate/down.py" = ["PLR0913"]
"python/confiture/cli/commands/migrate/fix_signatures.py" = ["PLR0913", "PLR2004"]
"python/confiture/cli/commands/migrate/generate.py" = ["PLR0913"]
"python/confiture/cli/commands/migrate/introspect.py" = ["PLR0912", "PLR0915"]
"python/confiture/cli/commands/migrate/preflight.py" = ["PLR0913"]
"python/confiture/cli/commands/migrate/rebuild.py" = ["C901", "PLR0913"]
"python/confiture/cli/commands/migrate/reinit.py" = ["C901", "PLR0912", "PLR2004"]
"python/confiture/cli/commands/migrate/status.py" = ["PLR0913"]
"python/confiture/cli/commands/migrate/up.py" = ["C901", "PLR0913"]
"python/confiture/cli/commands/migrate/validate.py" = ["PLR0913", "PLR2004"]
"python/confiture/cli/commands/migrate/verify.py" = ["PLR0913"]
"python/confiture/cli/commands/schema.py" = ["PLR0913", "PLR0915", "PLR2004"]
"python/confiture/cli/coordinate.py" = ["PLR0913"]
"python/confiture/cli/dry_run.py" = ["PLR2004"]
"python/confiture/cli/formatters/migrate_formatter.py" = ["C901", "PLR0912", "PLR0915", "PLR2004"]
"python/confiture/cli/formatters/validate_formatter.py" = ["C901", "PLR0912"]
"python/confiture/cli/generate.py" = ["PLR2004"]
"python/confiture/cli/idempotency.py" = ["C901", "PLR0912", "PLR2004"]
"python/confiture/cli/ownership.py" = ["C901", "PLR0912"]
"python/confiture/cli/seed.py" = ["C901", "PLR0912", "PLR0913", "PLR0915", "PLR2004"]
"python/confiture/cli/sync.py" = ["PLR0913"]
"python/confiture/config/environment.py" = ["C901", "PLR0912", "PLR0915"]
"python/confiture/core/_migrator/apply.py" = ["PLR2004"]
"python/confiture/core/_migrator/apply_loop.py" = ["C901", "PLR0912", "PLR0913"]
"python/confiture/core/_migrator/baseline.py" = ["PLR0913"]
"python/confiture/core/_migrator/session.py" = ["PLR0913"]
"python/confiture/core/anonymization/strategies/address.py" = ["PLR2004"]
"python/confiture/core/anonymization/strategies/credit_card.py" = ["PLR2004"]
"python/confiture/core/anonymization/strategies/differential_privacy.py" = ["PLR2004"]
"python/confiture/core/builder.py" = ["C901", "PLR0912", "PLR2004"]
"python/confiture/core/change_set/naming.py" = ["PLR2004"]
"python/confiture/core/differ.py" = ["PLR0911", "PLR2004"]
"python/confiture/core/dry_run.py" = ["PLR2004"]
"python/confiture/core/error_handler.py" = ["PLR0911"]
"python/confiture/core/fk_extractor.py" = ["C901", "PLR0912"]
"python/confiture/core/git_accompaniment.py" = ["PLR2004"]
"python/confiture/core/git_schema.py" = ["PLR2004"]
"python/confiture/core/hooks/notifications/transport.py" = ["PLR2004"]
"python/confiture/core/idempotency/_captures.py" = ["PLR2004"]
"python/confiture/core/idempotency/models.py" = ["PLR2004"]
"python/confiture/core/idempotency/static_eval/evaluator.py" = ["C901", "PLR0911", "PLR0912"]
"python/confiture/core/idempotency/static_eval/scope.py" = ["C901", "PLR0911", "PLR0912", "PLR0915"]
"python/confiture/core/idempotency/static_eval/str_methods.py" = ["C901", "PLR0911", "PLR0912", "PLR2004"]
"python/confiture/core/linting/inventory.py" = ["PLR2004"]
"python/confiture/core/linting/libraries/acl.py" = ["C901"]
"python/confiture/core/linting/libraries/functions.py" = ["PLR2004"]
"python/confiture/core/linting/libraries/gdpr.py" = ["PLR2004"]
"python/confiture/core/linting/libraries/general.py" = ["PLR2004"]
"python/confiture/core/linting/libraries/generate.py" = ["PLR2004"]
"python/confiture/core/linting/libraries/hipaa.py" = ["PLR2004"]
"python/confiture/core/linting/libraries/pci_dss.py" = ["PLR2004"]
"python/confiture/core/linting/libraries/security_definer.py" = ["PLR2004"]
"python/confiture/core/linting/libraries/sox.py" = ["PLR2004"]
"python/confiture/core/linting/schema_linter.py" = ["PLR0913", "PLR2004"]
"python/confiture/core/lock_profile.py" = ["PLR0911"]
"python/confiture/core/migration_generator.py" = ["PLR0911"]
"python/confiture/core/migration_grant_extractor.py" = ["C901", "PLR0912", "PLR0913", "PLR2004"]
"python/confiture/core/psql_applier.py" = ["C901", "PLR0912", "PLR0915", "PLR2004"]
"python/confiture/core/replica/classifier.py" = ["PLR0911"]
"python/confiture/core/replica/safety.py" = ["C901", "PLR0911", "PLR0912"]
"python/confiture/core/schema_facts.py" = ["PLR2004"]
"python/confiture/core/seed/applier.py" = ["C901", "PLR0912"]
"python/confiture/core/seed/insert_validator.py" = ["PLR0911"]
"python/confiture/core/seed/validation/models.py" = ["PLR2004"]
"python/confiture/core/seed/validation/prep_seed/level_1_seed_files.py" = ["PLR2004"]
"python/confiture/core/seed/validation/prep_seed/orchestrator.py" = ["PLR2004"]
"python/confiture/core/sql_utils.py" = ["C901", "PLR0912", "PLR0915"]
"python/confiture/core/temp_database.py" = ["PLR2004"]
"python/confiture/core/tree_allocator.py" = ["PLR2004"]
"python/confiture/core/tree_renumber.py" = ["PLR2004"]
"python/confiture/core/validation/signature_drift.py" = ["PLR2004"]
"python/confiture/core/view_manager.py" = ["PLR2004"]
"python/confiture/exceptions.py" = ["PLR2004"]
"python/confiture/integrations/pggit/client.py" = ["PLR2004"]
"python/confiture/integrations/pggit/exceptions.py" = ["PLR2004"]
"python/confiture/integrations/pggit/generator.py" = ["PLR2004"]
"python/confiture/models/schema.py" = ["C901", "PLR0911", "PLR0912"]
"python/confiture/testing/frameworks/mutation.py" = ["PLR2004"]
"python/confiture/testing/frameworks/performance.py" = ["PLR2004"]
# END GENERATED: budget-ignores

[tool.ty.src]
# Astral's ultra-fast type checker configuration
# Includes Python source code, excludes tests and examples
include = ["python"]
exclude = ["examples", "tests", "**/test_*.py"]

[tool.ty.environment]
python-version = "3.11"
root = ["python"]

[tool.ty.analysis]
respect-type-ignore-comments = true

[tool.ty.rules]
# psycopg3 uses strict type requirements (LiteralString for SQL queries).
# This library is designed to execute dynamic SQL from:
# - Migration files (trusted developer content)
# - Schema building (concatenated SQL files)
# - SAVEPOINT/ROLLBACK commands (safe, controlled strings)
# These are safe operations that can't be made type-safe without major refactoring.
invalid-argument-type = "ignore"

# Strategy configs use dynamic attribute access that the type checker can't verify.
# The base StrategyConfig class is extended by subclasses with additional fields,
# but the type system only sees the base class type in generic code.
possibly-missing-attribute = "ignore"

# psycopg 3.3+ types execute() to accept only LiteralString/SQL/Composed (an
# anti-SQL-injection guardrail). Confiture builds dynamic SQL from double-quoted,
# validated identifiers (each call site carries `# nosec B608`), so the runtime
# string never matches the overload. (Was `invalid-argument-type` under older ty.)
no-matching-overload = "ignore"

[[tool.ty.overrides]]
# pglast AST nodes are dynamically typed (Unknown | None); attribute access on
# them isn't statically resolvable. Scope the suppression to the AST-visitor
# modules so first-party attribute errors stay caught everywhere else.
include = [
    "python/confiture/core/migration_grant_extractor.py",
    "python/confiture/core/linting/libraries/ownership.py",
    "python/confiture/core/linting/libraries/functions.py",
    "python/confiture/core/replica/classifier.py",
]
[tool.ty.overrides.rules]
unresolved-attribute = "ignore"

[dependency-groups]
dev = [
    "maturin>=1.9.6",
    "ruff==0.15.15",
]
